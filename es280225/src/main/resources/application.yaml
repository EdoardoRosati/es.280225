spring:
  name: server-template
  datasource:
    url: jdbc:mysql://localhost:3306/es280225?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Deryltwd9
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update #none
    show-sql: true